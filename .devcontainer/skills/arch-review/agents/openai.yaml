interface:
  display_name: "Architecture Review"
  short_description: "Find latent architecture bugs; opt into broad review"
  default_prompt: "Use $arch-review for focused architecture-induced bug hunting, or add --mode=broad for an overall structural review."
policy:
  allow_implicit_invocation: false
