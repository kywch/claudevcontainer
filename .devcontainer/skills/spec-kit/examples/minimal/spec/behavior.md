# Behavior

This file is normative. If it conflicts with higher-precedence sources, the
higher source wins.

## B1 Count Tokens

B1.1 The count operation accepts one input string.

B1.2 A token is any non-empty span between ASCII whitespace delimiters.

B1.3 Empty input and delimiter-only input return count `0`.

### Traceability

| Decision | Schema | Conformance | Model | Notes |
| --- | --- | --- | --- | --- |
| DEC-0001 | n/a | n/a | n/a | Minimal draft example. |
